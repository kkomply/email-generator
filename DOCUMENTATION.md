# Документация по проекту Email Generator

## 1. Обзор проекта

**Email Generator** — это веб-приложение на русском языке, предназначенное для создания и персонализации email-шаблонов. Оно разделено на два основных режима: **Конструктор** (для администраторов) и **Рендерер** (для пользователей).

- **Конструктор (Builder):** В этом режиме администратор может создавать email-шаблоны, добавляя готовые блоки (текст, изображения, кнопки и т.д.). Ключевая особенность — возможность делать блоки "динамическими", превращая их в переменные с автоматической валидацией.
- **Рендерер (Renderer):** В этом режиме пользователь может заполнить форму, которая автоматически генерируется на основе динамических полей, созданных в конструкторе. После заполнения формы создается готовое, персонализированное HTML-письмо с предпросмотром в реальном времени.

## 2. Основные возможности

### Режим "Конструктор" (Режим администратора)

- **Визуальный редактор:** Создание шаблонов путем добавления блоков одним кликом.
- **Доступные блоки:**
  - 📝 Текстовый блок
  - 🖼️ Блок с изображением
  - 🔘 Кнопка
  - ➖ Разделитель
  - ⬜ Отступ
- **Настройка блоков:** Возможность изменять стили (цвет, размер шрифта, выравнивание, отступы, цвет фона) и содержимое каждого блока.
- **Динамические поля:** Любой блок (текст, изображение, кнопка) можно сделать динамическим. Для этого нужно:
    1. Выбрать блок на холсте
    2. Поставить галочку "Сделать динамическим"
    3. Заполнить обязательные поля:
       - **Имя переменной** (например, `client_name`) - только латинские буквы, цифры и подчеркивание. Пробелы автоматически заменяются на `_`
       - **Название поля** (например, "Имя клиента") - отображается в форме для пользователя
       - **Тип поля:**
         - Текст
         - Текстовая область
         - Число
         - Email
         - Выпадающий список (с возможностью указать варианты через запятую)
         - Чекбокс
       - **Обязательное поле** (опционально)
- **Валидация:** Система автоматически проверяет корректность имен переменных и предупреждает об ошибках.
- **Экспорт:** По завершении создания шаблона система экспортирует два файла:
    - `template.html`: HTML-структура письма с плейсхолдерами Handlebars (например, `{{client_name}}`).
    - `schema.json`: JSON-схема, описывающая все динамические поля для автоматической генерации формы.
- **Отладка:** В консоли браузера выводится детальная информация о созданных блоках и схеме при экспорте.

### Режим "Рендерер" (Режим пользователя)

- **Загрузка шаблона:** Пользователь загружает файлы `template.html` и `schema.json` (поддерживаются файлы с номерами, добавленными браузером: `schema (2).json`, `template (3).html`).
- **Умное определение файлов:** Система автоматически распознает нужные файлы даже если браузер добавил номер при повторном скачивании.
- **Статус загрузки:** Визуальная индикация процесса загрузки с сообщениями об ошибках или успехе.
- **Автоматическая генерация формы:** На основе `schema.json` на странице появляется форма со всеми необходимыми полями, соответствующими указанным типам.
- **Предпросмотр в реальном времени:** При заполнении формы справа отображается предпросмотр готового письма в iframe, где плейсхолдеры мгновенно заменяются введенными данными.
- **Копирование HTML:** Кнопка "Копировать HTML" позволяет скопировать готовый HTML-код письма в буфер обмена для использования в любой почтовой системе.

## 3. Технологический стек

- **Фреймворк:** React 19.2.0 (с использованием Hooks и функциональных компонентов)
- **Язык:** TypeScript 5.9.3
- **Сборщик:** Vite 7.2.4 с поддержкой Hot Module Replacement (HMR)
- **Маршрутизация:** React Router DOM 7.9.6
- **Генерация форм:** React JSON Schema Form (`@rjsf/core` 6.1.2, `@rjsf/validator-ajv8` 6.1.2)
- **Шаблонизатор:** Handlebars.js 4.7.8
- **Drag & Drop:** @dnd-kit/core 6.3.1 (готовность к будущему функционалу)
- **Линтер:** ESLint 9.39.1 с плагинами для React

## 4. Как запустить проект

### Требования
- Node.js (версия 18 или выше)
- npm или yarn

### Установка и запуск
```bash
# 1. Перейдите в директорию проекта
cd email-generator

# 2. Установите зависимости
npm install

# 3. Запустите сервер разработки
npm run dev

# 4. Откройте браузер по адресу, указанному в консоли
# Обычно: http://localhost:5173 (или другой порт, если 5173 занят)
```

### Другие команды
```bash
# Сборка для продакшена
npm run build

# Проверка типов TypeScript
tsc -b

# Запуск линтера
npm run lint

# Предпросмотр production сборки
npm run preview
```

## 5. Структура проекта

```
email-generator/
├── src/
│   ├── components/
│   │   ├── Builder/
│   │   │   ├── BlockList.tsx          # Список доступных блоков
│   │   │   ├── BlockList.css
│   │   │   ├── Canvas.tsx             # Холст для размещения блоков
│   │   │   ├── Canvas.css
│   │   │   ├── PropertiesPanel.tsx    # Панель свойств блока
│   │   │   ├── PropertiesPanel.css
│   │   │   ├── ExportButton.tsx       # Кнопка экспорта шаблона
│   │   │   └── ExportButton.css
│   │   └── Renderer/
│   │       ├── FormRenderer.tsx       # Генератор формы из JSON Schema
│   │       ├── FormRenderer.css
│   │       ├── EmailPreview.tsx       # Предпросмотр письма с Handlebars
│   │       └── EmailPreview.css
│   ├── pages/
│   │   ├── BuilderPage.tsx            # Страница конструктора
│   │   ├── BuilderPage.css
│   │   ├── RendererPage.tsx           # Страница рендерера
│   │   └── RendererPage.css
│   ├── types/
│   │   └── index.ts                   # TypeScript типы и интерфейсы
│   ├── App.tsx                        # Главный компонент с роутингом
│   ├── App.css
│   ├── main.tsx                       # Точка входа приложения
│   └── index.css
├── public/                            # Статические файлы
├── package.json                       # Зависимости проекта
├── tsconfig.json                      # Конфигурация TypeScript
├── vite.config.ts                     # Конфигурация Vite
├── eslint.config.js                   # Конфигурация ESLint
├── DOCUMENTATION.md                   # Эта документация
└── README.md                          # Базовая информация о проекте
```

## 6. Использование приложения

### Сценарий 1: Создание шаблона (Администратор)

1. Откройте приложение и перейдите в режим "Конструктор"
2. Добавьте нужные блоки, нажимая на них в левой панели
3. Выберите блок на холсте для редактирования
4. Настройте содержимое и стили в правой панели
5. Для создания динамического поля:
   - Поставьте галочку "Сделать динамическим"
   - Введите имя переменной (например: `user_name`, `company_title`)
   - Укажите название поля для пользователя (например: "Имя пользователя")
   - Выберите тип поля
   - При необходимости отметьте "Обязательное поле"
6. Нажмите "Экспорт шаблона"
7. Скачаются два файла: `template.html` и `schema.json`

### Сценарий 2: Заполнение формы (Пользователь)

1. Откройте приложение и перейдите в режим "Рендерер"
2. Нажмите на поле выбора файлов
3. Выберите оба файла (`template.html` и `schema.json`) одновременно (Ctrl/Cmd + клик)
4. Дождитесь сообщения "Оба файла успешно загружены!"
5. Заполните форму слева
6. Наблюдайте предпросмотр письма справа в реальном времени
7. Когда письмо готово, нажмите "Копировать HTML"
8. Используйте скопированный HTML в вашей почтовой системе

## 7. Особенности реализации

### Валидация имен переменных
Система автоматически санитизирует имена переменных:
- Удаляет все символы кроме латинских букв, цифр и подчеркивания
- Пробелы автоматически заменяются на `_`
- Примеры преобразований:
  - `client name` → `client_name`
  - `user-email` → `user_email`
  - `product.price` → `product_price`

### Умное определение файлов
Рендерер распознает файлы даже если браузер добавил номер при повторном скачивании:
- `schema.json` ✅
- `schema (2).json` ✅
- `schema (10).json` ✅
- `template.html` ✅
- `template (3).html` ✅

### Отладочная информация
При экспорте шаблона в консоль браузера (F12) выводится:
- Список всех блоков
- Сгенерированная JSON Schema
- Список имен переменных
- Список обязательных полей

Это помогает отлаживать шаблоны и проверять корректность данных.

### Интерфейс на русском языке
Все элементы интерфейса переведены на русский язык для удобства русскоязычных пользователей.

## 8. Известные ограничения

1. **Требуется заполнение имени переменной:** При создании динамического поля обязательно нужно заполнить "Имя переменной", иначе поле не попадет в schema.json
2. **Два файла обязательны:** Для работы рендерера необходимы оба файла - template.html и schema.json
3. **Ограничение символов:** Имена переменных могут содержать только латинские буквы, цифры и подчеркивание

## 9. Планы развития

- [ ] Drag & Drop функционал для перемещения блоков
- [ ] Сохранение шаблонов в localStorage
- [ ] Экспорт в различные форматы (PDF, PNG)
- [ ] Библиотека готовых шаблонов
- [ ] Поддержка условной логики в шаблонах
- [ ] Интеграция с почтовыми сервисами
- [ ] Мультиязычность (поддержка нескольких языков)
- [ ] Темная тема интерфейса
